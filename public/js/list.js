// 상세보기로 이동
function detail(id) {
  location.href = "/accountBook/detail?id=" + id;
}

// 수정하기로 이동
function update(id) {
  // console.log("aaa");
  location.href = "/accountBook/update?id=" + id;
}

// 삭제하기
function remove(id) {
  location.href
}